--- a/ui/qt/BUILD.gn
+++ b/ui/qt/BUILD.gn
@@ -61,10 +61,29 @@ template("qt_shim") {
     }
   }
 
-  pkg_config("qt" + invoker.qt_version + "_config") {
-    packages = [
-      "Qt" + invoker.qt_version + "Core",
-      "Qt" + invoker.qt_version + "Widgets",
-    ]
+  # PATCHED: For cross-compilation, use custom config instead of pkg_config
+  if (!use_sysroot && target_cpu != "x64") {
+    config("qt" + invoker.qt_version + "_config") {
+      # Cross-compilation: use system Qt6 headers and link against runtime Qt6
+      defines = [
+        "QT_CORE_LIB",
+        "QT_GUI_LIB",
+        "QT_WIDGETS_LIB",
+      ]
+      include_dirs = [
+        "/usr/include/qt6",
+        "/usr/include/qt6/QtCore",
+        "/usr/include/qt6/QtGui",
+        "/usr/include/qt6/QtWidgets",
+      ]
+      # Link against Qt6 libraries (will be resolved at runtime on target system)
+      libs = [ "Qt6Core", "Qt6Gui", "Qt6Widgets" ]
+    }
+  } else {
+    pkg_config("qt" + invoker.qt_version + "_config") {
+      packages = [
+        "Qt" + invoker.qt_version + "Core",
+        "Qt" + invoker.qt_version + "Widgets",
+      ]
+    }
   }
 
