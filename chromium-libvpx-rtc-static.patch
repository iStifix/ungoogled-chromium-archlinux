--- a/build/linux/unbundle/libvpx.gn
+++ b/build/linux/unbundle/libvpx.gn
@@ -51,13 +51,11 @@
   ]
 }

-# Stub for VA-API dependency - system libvpx already contains full encoder implementation
-# including rate control functionality needed by VA-API hardware encoding
+# RTC library for VA-API hardware encoding
+# Uses static libraries from //deps/lib/ with full RTC API support
 source_set("libvpxrc") {
-  # No sources needed - system libvpx.so contains all necessary symbols
-  public_deps = [ ":libvpx" ]
   public_configs = [
-    ":system_libvpx",
     ":libvpx_rc_link_settings",
+    ":libvpx_internal_headers",
   ]
 }
