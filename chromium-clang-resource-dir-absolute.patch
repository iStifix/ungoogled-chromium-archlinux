diff --git a/build/rust/rust_bindgen_generator.gni b/build/rust/rust_bindgen_generator.gni
index 000000000000..111111111111 100644
--- a/build/rust/rust_bindgen_generator.gni
+++ b/build/rust/rust_bindgen_generator.gni
@@ -225,9 +225,11 @@
       args += [ "--ld-library-path", rust_sysroot_lib_path ]
     }

-    clang_resource_dir =
-        rebase_path(clang_base_path + "/lib/clang/" + clang_version,
-                    root_build_dir)
+    # Patched for cross-compilation: use absolute path instead of relative
+    # because bindgen with relative paths cannot find clang headers
+    # clang_resource_dir = rebase_path(clang_base_path + "/lib/clang/" + clang_version, root_build_dir)
+    clang_resource_dir = clang_base_path + "/lib/clang/" + clang_version
+
     args += [
       "-resource-dir",
       clang_resource_dir,