--- a/build/gn_run_binary.py
+++ b/build/gn_run_binary.py
@@ -21,7 +21,21 @@ if not os.path.isabs(path):

 # The rest of the arguments are passed directly to the executable.
 args = [path] + sys.argv[2:]

-ret = subprocess.call(args)
+# Add Debian sysroot libraries to LD_LIBRARY_PATH for cross-compilation
+# This is needed for tools like v8_context_snapshot_generator which are
+# built for x86_64 host but linked against Debian Bullseye sysroot
+env = os.environ.copy()
+sysroot_base = os.path.join(
+    os.path.dirname(os.path.dirname(os.path.abspath(__file__))),
+    "build", "linux", "debian_bullseye_amd64-sysroot"
+)
+sysroot_lib = os.path.join(sysroot_base, "usr", "lib", "x86_64-linux-gnu")
+sysroot_lib2 = os.path.join(sysroot_base, "lib", "x86_64-linux-gnu")
+if os.path.exists(sysroot_lib) and os.path.exists(sysroot_lib2):
+    env["LD_LIBRARY_PATH"] = sysroot_lib + ":" + sysroot_lib2 + ":" + env.get("LD_LIBRARY_PATH", "")
+
+ret = subprocess.call(args, env=env)
 if ret != 0:
   if ret <= -100:
     # Windows error codes such as 0xC0000005 and 0xC0000409 are much easier to
